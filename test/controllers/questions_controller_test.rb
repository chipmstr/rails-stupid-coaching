require "test_helper"

class QuestionsControllerTest < ActionDispatch::IntegrationTest
  # Add tests here for the QuestionsController actions / I tried, but it did not work, because it did not find chrome. don´t know why.
end
